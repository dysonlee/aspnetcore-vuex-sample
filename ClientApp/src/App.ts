import Vue from 'vue';
import RollerLoading from '@/components/system/RollerLoading.vue';
import SnackbarMessage from '@/components/system/SnackbarMessage.vue';

export default Vue.extend({
    name: 'App',
    components: {
        RollerLoading,
        SnackbarMessage,
    },
});
